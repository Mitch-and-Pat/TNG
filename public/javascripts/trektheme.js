// Theme, sounds, etc.
$(document).ready(function() {
  console.log("-- This Script is Running --");


  //Control .transmissions_ol programatically
    //Set the transmissions_ol as a jQuery object
    //var $test = $(".transmissions_ol");

    //var $children = $test.children();

    //$children.hide();



    //var $streamList = $stream.children();
    //var $firstTweet = $streamList[0];

    //$firstTweet.show();

    //Hide the "previous" button
        //$("#previous-tweet").hide();

    //Hide all li's
    //$stream.children().hide();
    //Get the current number of list items in transmissions_ol
    //Get the first li in transmissions
    //Hide the visibility of all of the li's
    //Set #tweet-number to current number of this li
    //Set #total-tweet to the total number of li's in the ol
    //Display this tweet

    //Put event handler on the "next" button
      //Hide the currently shown li
      //Activate the load-new-tweet sequence
      //Show the new tweet
      //Show the "previous" button

    //Put event handler on the "previous" button
      //Hide the currently shown li
      //Activate the load-new-tweet sequence
      //If the current
      //Show the new tweet

});
